# State-based Patterns(状态模式) | AWS 流程引擎对WCP的支持评估

# State-based Patterns(状态模式)

状态模式描述的是在支持状态的流程语言中最容易实现。在这些模式中，我们认为一个流程实例的状态，包括与当前执行流程相关的数据的集合，包括各种活动的状态，以及流程相关的工作数据，如活动和实例的数据元素。

  * [WCP16-Deferred Choice(延迟选择)](<./wcp-16.html>)
  * [WCP17-Interleaved Parallel Routing(并行交叉路由)](<./wcp-17.html>)
  * [WCP18-Milestone(里程碑)](<./wcp-18.html>)
  * [WCP39-Critical Section(独占)](<./wcp-39.html>)
  * [WCP40-Interleaved Routing(交叉路由)](<./wcp-40.html>)